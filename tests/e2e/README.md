# Forge end-to-end tests

The deployed Cloudflare acceptance suite belongs here. It is intentionally not faked by local tests because Sandbox provisioning, Workflows retries, Browser Run and preview routing require real Cloudflare resources. See `docs/operations/runbook.md`.
