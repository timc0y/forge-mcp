{
  "name": "forge-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.13.1",
  "engines": {
    "node": ">=22 <27"
  },
  "scripts": {
    "dev": "pnpm --dir worker dev",
    "typecheck": "pnpm --dir worker typecheck",
    "test": "pnpm --dir worker test",
    "check": "pnpm typecheck && pnpm test",
    "deploy": "pnpm --dir worker run deploy"
  }
}
