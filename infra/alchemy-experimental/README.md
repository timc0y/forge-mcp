# Alchemy infrastructure experiment

This directory is intentionally non-production. Canonical infrastructure remains under `infra/wrangler`.

Alchemy may be introduced only after it passes resource-parity, preview-environment, destroy, drift, secret-handling and disaster-recovery tests. The `alchemy_infra_experimental` feature flag remains off and there is no production dependency on this directory.
