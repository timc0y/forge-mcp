{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Forge MCP tool schemas",
  "version": 1,
  "generatedAt": "deterministic",
  "tools": {
    "forge_capabilities": {
      "title": "Get Forge capability manifest",
      "description": "Return the stable repository, workspace, Git, recovery, browser, deployment, and approval capabilities available to this MCP session.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    "forge_credential_list": {
      "title": "List credential profiles",
      "description": "List the tenant credential profiles. Secrets and encrypted payloads are never returned.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    "forge_credential_create": {
      "title": "Create credential profile",
      "description": "Encrypt and store a Cloudflare API token as a named tenant profile. The secret is never returned or logged.",
      "sideEffect": "external",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "provider": {
            "type": "string",
            "const": "cloudflare"
          },
          "secret": {
            "type": "string",
            "minLength": 16,
            "maxLength": 4096
          },
          "metadata": {
            "default": {},
            "type": "object",
            "properties": {
              "account_id": {
                "type": "string",
                "pattern": "^[a-f0-9]{32}$"
              }
            },
            "additionalProperties": false
          },
          "make_active": {
            "default": false,
            "type": "boolean"
          }
        },
        "required": [
          "name",
          "provider",
          "secret",
          "metadata",
          "make_active"
        ],
        "additionalProperties": false
      }
    },
    "forge_credential_update": {
      "title": "Update credential profile",
      "description": "Rename, replace, or update the metadata of a credential profile. Replacing a secret resets validation state.",
      "sideEffect": "external",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "credential_profile_id": {
            "type": "string",
            "pattern": "^crp_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Tenant credential profile id (crp_...)."
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "secret": {
            "type": "string",
            "minLength": 16,
            "maxLength": 4096
          },
          "metadata": {
            "default": {},
            "type": "object",
            "properties": {
              "account_id": {
                "type": "string",
                "pattern": "^[a-f0-9]{32}$"
              }
            },
            "additionalProperties": false
          },
          "make_active": {
            "type": "boolean"
          }
        },
        "required": [
          "credential_profile_id"
        ],
        "additionalProperties": false
      }
    },
    "forge_credential_delete": {
      "title": "Delete credential profile",
      "description": "Permanently delete a tenant credential profile.",
      "sideEffect": "destructive",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "credential_profile_id": {
            "type": "string",
            "pattern": "^crp_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Tenant credential profile id (crp_...)."
          }
        },
        "required": [
          "credential_profile_id"
        ],
        "additionalProperties": false
      }
    },
    "forge_credential_switch": {
      "title": "Switch credential profile",
      "description": "Select a profile for this MCP session and mark it active for the tenant.",
      "sideEffect": "workspace",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "credential_profile_id": {
            "type": "string",
            "pattern": "^crp_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Tenant credential profile id (crp_...)."
          }
        },
        "required": [
          "credential_profile_id"
        ],
        "additionalProperties": false
      }
    },
    "forge_credential_validate": {
      "title": "Validate credential profile",
      "description": "Decrypt a profile only for its provider validation request, then persist its validation state.",
      "sideEffect": "external",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "credential_profile_id": {
            "type": "string",
            "pattern": "^crp_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Tenant credential profile id (crp_...)."
          }
        },
        "required": [
          "credential_profile_id"
        ],
        "additionalProperties": false
      }
    },
    "forge_secret_list": {
      "title": "List secrets",
      "description": "List stored secrets with their labels, providers, and environment variable names. Values are never returned.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    "forge_secret_create": {
      "title": "Create a secret",
      "description": "Store environment variables as a named, labeled secret. Values are encrypted at rest and never returned. Labels must be unique per tenant.",
      "sideEffect": "external",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "label": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100,
            "description": "Human-readable label, e.g. \"CF Production\" or \"Shopify - Store A\"."
          },
          "provider": {
            "default": "generic",
            "type": "string",
            "enum": [
              "cloudflare",
              "shopify",
              "generic"
            ]
          },
          "env": {
            "type": "object",
            "propertyNames": {
              "$ref": "#/$defs/__schema0"
            },
            "additionalProperties": {
              "$ref": "#/$defs/__schema1"
            },
            "description": "Environment variable key-value pairs to store."
          }
        },
        "required": [
          "label",
          "provider",
          "env"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string"
          },
          "__schema1": {
            "type": "string"
          }
        }
      }
    },
    "forge_secret_update": {
      "title": "Update a secret",
      "description": "Update a stored secret's label, provider, or environment variables. Replacing env vars resets validation state.",
      "sideEffect": "external",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "secret_id": {
            "type": "string",
            "pattern": "^sec_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Secret id (sec_...)."
          },
          "label": {
            "description": "New label.",
            "$ref": "#/$defs/__schema0"
          },
          "provider": {
            "default": "generic",
            "type": "string",
            "enum": [
              "cloudflare",
              "shopify",
              "generic"
            ]
          },
          "env": {
            "type": "object",
            "propertyNames": {
              "$ref": "#/$defs/__schema1"
            },
            "additionalProperties": {
              "$ref": "#/$defs/__schema2"
            },
            "description": "Environment variable key-value pairs to store."
          }
        },
        "required": [
          "secret_id"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "__schema1": {
            "type": "string"
          },
          "__schema2": {
            "type": "string"
          }
        }
      }
    },
    "forge_secret_delete": {
      "title": "Delete a secret",
      "description": "Permanently delete a stored secret and detach it from any workspaces.",
      "sideEffect": "destructive",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "secret_id": {
            "type": "string",
            "pattern": "^sec_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Secret id (sec_...)."
          }
        },
        "required": [
          "secret_id"
        ],
        "additionalProperties": false
      }
    },
    "forge_secret_attach": {
      "title": "Attach secret to workspace",
      "description": "Attach a stored secret's environment variables to a workspace. After human approval, the variables are injected into every command in that workspace. Retry with the returned approval_id once approved.",
      "sideEffect": "external",
      "approval": "required",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "secret_id": {
            "type": "string",
            "pattern": "^sec_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Secret id (sec_...)."
          },
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "approval_id": {
            "description": "Approval id from a prior approval page.",
            "$ref": "#/$defs/__schema1"
          }
        },
        "required": [
          "secret_id"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "string",
            "pattern": "^apr_.*"
          }
        }
      }
    },
    "forge_secret_detach": {
      "title": "Detach secret from workspace",
      "description": "Remove a stored secret's environment variables from a workspace.",
      "sideEffect": "workspace",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "secret_id": {
            "type": "string",
            "pattern": "^sec_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Secret id (sec_...)."
          },
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          }
        },
        "required": [
          "secret_id"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          }
        }
      }
    },
    "forge_workspace_reconcile": {
      "title": "Reconcile workspace recovery state",
      "description": "Inspect and reconcile the workspace Git state, processes, and dependency state. Stops or adopts unknown processes, checks for Git lock files, verifies .git/HEAD and index readability, compares HEAD and branch with recorded values, detects package-manager activity, and restores a checkpoint only when necessary. Returns a detailed gitIntegrity report with state, reason, blocking process, and recommended action. Safe to call when Git inspection fails — it never throws, it reports.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          }
        },
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          }
        }
      }
    },
    "forge_operation_get": {
      "title": "Get operation status",
      "description": "Reconcile an uncertain prior mutation by operation id. Syncs process lifecycle first, then returns whether the operation was accepted, is still active (e.g. managed process running), or completed/failed — without inferring from side effects alone.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "operation_id": {
            "type": "string",
            "pattern": "^op_.*",
            "description": "Operation id from a prior mutating tool response."
          }
        },
        "required": [
          "operation_id"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          }
        }
      }
    },
    "forge_operation_reconcile": {
      "title": "Reconcile operation outcome",
      "description": "Same as forge_operation_get: after a client disconnect or unknown tool outcome, ask whether operation op_... finished, failed, or remains active. Syncs managed-process lifecycle before answering.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "operation_id": {
            "type": "string",
            "pattern": "^op_.*",
            "description": "Operation id from a prior mutating tool response."
          }
        },
        "required": [
          "operation_id"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          }
        }
      }
    },
    "forge_workspace_prove": {
      "title": "Prove workspace state",
      "description": "Produce one evidence receipt from the actual checkout: immutable base, branch, HEAD, changed paths, filesystem and HEAD file hashes, worktree hash, committed outgoing hash, and recorded-state divergence.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          }
        },
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          }
        }
      }
    },
    "forge_workspace_checkpoint": {
      "title": "Create durable workspace checkpoint",
      "description": "Create a provider snapshot containing the workspace filesystem and Git data before a risky operation. The checkpoint is retained independently from the active session.",
      "sideEffect": "workspace",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 120
          }
        },
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          }
        }
      }
    },
    "forge_workspace_restore": {
      "title": "Restore durable workspace checkpoint",
      "description": "Restore a recorded provider checkpoint into this workspace. Forge blocks restoration over dirty or unpushed work so recovery cannot silently discard a newer change.",
      "sideEffect": "destructive",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "snapshot_id": {
            "type": "string",
            "pattern": "^snap_.*"
          },
          "expected_revision": {
            "description": "Optimistic-concurrency guard: the revision you expect; the call fails if the workspace or task has moved on.",
            "$ref": "#/$defs/__schema1"
          }
        },
        "required": [
          "snapshot_id"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 9007199254740991
          }
        }
      }
    },
    "forge_work_export": {
      "title": "Export recoverable work",
      "description": "Persist an evidence-backed recovery bundle to Forge artifacts before risky Git, session, or teardown operations. The export contains committed and uncommitted binary diffs plus a base64 tar.gz archive of all untracked files.",
      "sideEffect": "workspace",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "max_bytes": {
            "default": 3000000,
            "type": "integer",
            "minimum": 1000,
            "maximum": 4000000
          },
          "idempotency_key": {
            "description": "Optional. Unique key that makes this mutation safe to retry; supply a stable value to make a retry idempotent, or omit it to always execute.",
            "$ref": "#/$defs/__schema1"
          }
        },
        "required": [
          "max_bytes"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "string",
            "minLength": 8,
            "maxLength": 200,
            "description": "Unique key that makes this mutation safe to retry; use a fresh value per distinct call."
          }
        }
      }
    },
    "forge_cloudflare_deploy": {
      "title": "Deploy with Cloudflare Wrangler",
      "description": "Deploy the workspace with pnpm exec wrangler using the selected Cloudflare profile for this one approved command. The API token is redacted from all results and is never persisted in the sandbox.",
      "sideEffect": "external",
      "approval": "required",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "credential_profile_id": {
            "type": "string",
            "pattern": "^crp_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Tenant credential profile id (crp_...)."
          },
          "environment": {
            "type": "string",
            "pattern": "^[A-Za-z0-9_-]{1,64}$"
          },
          "config_path": {
            "type": "string",
            "maxLength": 500,
            "pattern": "^\\/workspace\\/repo\\/.*"
          },
          "approval_id": {
            "type": "string",
            "pattern": "^apr_.*"
          },
          "expected_revision": {
            "description": "Optimistic-concurrency guard: the revision you expect; the call fails if the workspace or task has moved on.",
            "$ref": "#/$defs/__schema1"
          },
          "idempotency_key": {
            "description": "Optional. Unique key that makes this mutation safe to retry; supply a stable value to make a retry idempotent, or omit it to always execute.",
            "$ref": "#/$defs/__schema2"
          }
        },
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 9007199254740991
          },
          "__schema2": {
            "type": "string",
            "minLength": 8,
            "maxLength": 200,
            "description": "Unique key that makes this mutation safe to retry; use a fresh value per distinct call."
          }
        }
      }
    },
    "forge_repository_list": {
      "title": "List authorized repositories",
      "description": "List the GitHub repositories authorized for this account through the Forge GitHub App. Container-free — use it to pick a repository before starting a task or workspace.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    "forge_task_start": {
      "title": "Start a task",
      "description": "Create a durable task record that survives MCP reconnects, context compression and container sleep. Container-free — start a task first for any coherent piece of work, then attach one workspace later only when you need to run or edit code.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "repository": {
            "type": "object",
            "properties": {
              "provider": {
                "$ref": "#/$defs/__schema0"
              },
              "owner": {
                "$ref": "#/$defs/__schema1"
              },
              "name": {
                "$ref": "#/$defs/__schema2"
              }
            },
            "required": [
              "provider",
              "owner",
              "name"
            ],
            "additionalProperties": false,
            "description": "Authorized GitHub repository to act on."
          },
          "base_ref": {
            "default": "main",
            "description": "The branch or ref the task branches from.",
            "$ref": "#/$defs/__schema3"
          },
          "goal": {
            "type": "string",
            "minLength": 1,
            "maxLength": 2000,
            "description": "What this task should achieve."
          },
          "decisions": {
            "default": [],
            "description": "Durable decisions that must survive context compression.",
            "$ref": "#/$defs/__schema4"
          },
          "non_goals": {
            "default": [],
            "description": "Things this task deliberately will not do.",
            "$ref": "#/$defs/__schema6"
          },
          "likely_paths": {
            "default": [],
            "description": "Files or directories the work is expected to touch.",
            "$ref": "#/$defs/__schema8"
          }
        },
        "required": [
          "repository",
          "base_ref",
          "goal",
          "decisions",
          "non_goals",
          "likely_paths"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "const": "github"
          },
          "__schema1": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "__schema2": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "__schema3": {
            "type": "string",
            "minLength": 1,
            "maxLength": 255
          },
          "__schema4": {
            "maxItems": 40,
            "type": "array",
            "items": {
              "$ref": "#/$defs/__schema5"
            }
          },
          "__schema5": {
            "type": "string",
            "minLength": 1,
            "maxLength": 500
          },
          "__schema6": {
            "maxItems": 40,
            "type": "array",
            "items": {
              "$ref": "#/$defs/__schema7"
            }
          },
          "__schema7": {
            "type": "string",
            "minLength": 1,
            "maxLength": 500
          },
          "__schema8": {
            "maxItems": 40,
            "type": "array",
            "items": {
              "$ref": "#/$defs/__schema9"
            }
          },
          "__schema9": {
            "type": "string",
            "minLength": 1,
            "maxLength": 500
          }
        }
      }
    },
    "forge_task_get": {
      "title": "Get a task",
      "description": "Get the full task record: branch, workspace, previews, files read and changed, checks and evidence ids. Read-only and container-free.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "task_id": {
            "type": "string",
            "pattern": "^task_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target task id (task_...)."
          },
          "compact": {
            "description": "Return a token-efficient compact summary.",
            "$ref": "#/$defs/__schema0"
          }
        },
        "required": [
          "task_id"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "boolean"
          }
        }
      }
    },
    "forge_task_summary": {
      "title": "Summarize a task",
      "description": "Get a compact resume summary for a task — goal, decisions, non-goals, state, files, checks, evidence, outstanding work and the next recommended action — so a fresh turn can continue without replaying the session. Excludes source, logs, diffs and secrets. Container-free.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "task_id": {
            "type": "string",
            "pattern": "^task_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target task id (task_...)."
          },
          "compact": {
            "description": "Return a token-efficient compact summary.",
            "$ref": "#/$defs/__schema0"
          }
        },
        "required": [
          "task_id"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "boolean"
          }
        }
      }
    },
    "forge_task_handoff": {
      "title": "Record task handoff",
      "description": "Record a structured handoff note on a task so another agent or a fresh ChatGPT chat session can resume work cleanly without context loss.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "task_id": {
            "type": "string",
            "pattern": "^task_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target task id (task_...)."
          },
          "summary": {
            "type": "string",
            "minLength": 1,
            "maxLength": 2000,
            "description": "Progress summary so far."
          },
          "next_steps": {
            "minItems": 1,
            "maxItems": 20,
            "type": "array",
            "items": {
              "$ref": "#/$defs/__schema0"
            },
            "description": "Actionable next steps for the resuming agent."
          },
          "key_learnings": {
            "description": "Key architecture or bug findings discovered during this session.",
            "$ref": "#/$defs/__schema1"
          },
          "modified_files": {
            "description": "Key paths edited or intended for modification.",
            "$ref": "#/$defs/__schema3"
          },
          "blocked_by": {
            "description": "Any blocker or approval currently required.",
            "$ref": "#/$defs/__schema5"
          }
        },
        "required": [
          "task_id",
          "summary",
          "next_steps"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "minLength": 1,
            "maxLength": 500
          },
          "__schema1": {
            "maxItems": 20,
            "type": "array",
            "items": {
              "$ref": "#/$defs/__schema2"
            }
          },
          "__schema2": {
            "type": "string",
            "minLength": 1,
            "maxLength": 500
          },
          "__schema3": {
            "maxItems": 50,
            "type": "array",
            "items": {
              "$ref": "#/$defs/__schema4"
            }
          },
          "__schema4": {
            "type": "string",
            "minLength": 1,
            "maxLength": 500
          },
          "__schema5": {
            "type": "string",
            "maxLength": 500
          }
        }
      }
    },
    "forge_task_resume": {
      "title": "Resume task in fresh session",
      "description": "Single-turn recovery for a fresh ChatGPT session or new agent. Returns task goal, active workspace state, latest Git diff summary, checks, and handoff notes in one compact response.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "task_id": {
            "type": "string",
            "pattern": "^task_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target task id (task_...)."
          },
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "compact": {
            "default": true,
            "description": "Return a tight, context-optimized summary for fast turn recovery.",
            "$ref": "#/$defs/__schema1"
          }
        },
        "required": [
          "task_id",
          "compact"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "boolean"
          }
        }
      }
    },
    "forge_context_pack": {
      "title": "Pack bounded context",
      "description": "Extract and pack essential file signatures, exports, instructions, and path context into a single token-optimized context block for ChatGPT turn initialization. Container-free.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "repository": {
            "type": "object",
            "properties": {
              "provider": {
                "$ref": "#/$defs/__schema0"
              },
              "owner": {
                "$ref": "#/$defs/__schema1"
              },
              "name": {
                "$ref": "#/$defs/__schema2"
              }
            },
            "required": [
              "provider",
              "owner",
              "name"
            ],
            "additionalProperties": false,
            "description": "Authorized GitHub repository to act on."
          },
          "goal": {
            "type": "string",
            "minLength": 1,
            "maxLength": 2000
          },
          "paths": {
            "maxItems": 20,
            "type": "array",
            "items": {
              "$ref": "#/$defs/__schema3"
            }
          },
          "task_id": {
            "type": "string",
            "pattern": "^task_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target task id (task_...)."
          },
          "max_tokens": {
            "default": 4000,
            "description": "Approximate token budget for the packed output.",
            "$ref": "#/$defs/__schema4"
          }
        },
        "required": [
          "repository",
          "goal",
          "max_tokens"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "const": "github"
          },
          "__schema1": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "__schema2": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "__schema3": {
            "type": "string",
            "minLength": 1,
            "maxLength": 500
          },
          "__schema4": {
            "type": "integer",
            "minimum": 500,
            "maximum": 10000
          }
        }
      }
    },
    "forge_task_list": {
      "title": "List tasks",
      "description": "List recent tasks for the account, most recently updated first, optionally filtered by state or a free-text query. Container-free.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "state": {
            "description": "Only return tasks in this state.",
            "$ref": "#/$defs/__schema0"
          },
          "q": {
            "description": "Free-text filter matched against the goal, repository, and task details (changed files, decisions, outstanding work).",
            "$ref": "#/$defs/__schema1"
          },
          "limit": {
            "default": 20,
            "description": "Maximum number of tasks to return.",
            "$ref": "#/$defs/__schema2"
          }
        },
        "required": [
          "limit"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "enum": [
              "planning",
              "ready",
              "coding",
              "validating",
              "previewing",
              "reviewing",
              "awaiting-approval",
              "complete",
              "failed",
              "cancelled"
            ]
          },
          "__schema1": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200
          },
          "__schema2": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          }
        }
      }
    },
    "forge_task_finish": {
      "title": "Finish a task",
      "description": "Move a task to a terminal state (complete, failed or cancelled). Container-free. Finishing as 'complete' is refused when failed/partial checks, unrecorded checks, unpushed changed files or outstanding items are on record — pass force with a note explaining what remains unverified to override.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "task_id": {
            "type": "string",
            "pattern": "^task_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target task id (task_...)."
          },
          "outcome": {
            "type": "string",
            "enum": [
              "complete",
              "failed",
              "cancelled"
            ],
            "description": "The terminal state to move the task to."
          },
          "note": {
            "description": "Optional closing note added to the task. Required when force is true.",
            "$ref": "#/$defs/__schema0"
          },
          "force": {
            "default": false,
            "description": "Override the completion-gap check for outcome complete. Requires note explaining what is unverified.",
            "$ref": "#/$defs/__schema1"
          },
          "expected_revision": {
            "description": "Optimistic-concurrency guard: the revision you expect; the call fails if the workspace or task has moved on.",
            "$ref": "#/$defs/__schema2"
          }
        },
        "required": [
          "task_id",
          "outcome",
          "force"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "maxLength": 2000
          },
          "__schema1": {
            "type": "boolean"
          },
          "__schema2": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 9007199254740991
          }
        }
      }
    },
    "forge_review": {
      "title": "Review a deployed URL",
      "description": "Screenshot any live public URL and get the images back in this one call — the cheapest and most reliable path, no container and nothing to poll. Pass just a url to capture it at phone and desktop; add captures to cover more routes. Always returns whatever it managed to capture rather than failing outright. Each cell reports a structure-health signal (structureSummary and evidence[].accessibility.structure) that flags heading defects a screenshot hides — stacked, empty, duplicate or skipped-level headings — which must be resolved or explicitly accepted before the review passes.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "format": "uri",
            "description": "The public URL to review."
          },
          "captures": {
            "default": [
              {
                "path": "/",
                "state": "entry"
              }
            ],
            "description": "Routes to capture. Omit to capture the URL as given.",
            "$ref": "#/$defs/__schema0"
          },
          "viewports": {
            "default": [
              "phone",
              "desktop"
            ],
            "description": "Viewports to capture each route at. Use the shorthand names 'phone', 'tablet' or 'desktop', or give explicit sizes.",
            "$ref": "#/$defs/__schema4"
          },
          "full_page": {
            "default": false,
            "description": "Capture the full scrollable page instead of just the viewport.",
            "$ref": "#/$defs/__schema6"
          },
          "time_budget_ms": {
            "default": 45000,
            "description": "Give up capturing after this long and return what succeeded. The default suits chat clients, which abandon a slow tool call and leave you with nothing; raise it when the caller can wait.",
            "$ref": "#/$defs/__schema7"
          }
        },
        "required": [
          "url",
          "captures",
          "viewports",
          "full_page",
          "time_budget_ms"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "minItems": 1,
            "maxItems": 10,
            "type": "array",
            "items": {
              "$ref": "#/$defs/__schema1"
            }
          },
          "__schema1": {
            "type": "object",
            "properties": {
              "path": {
                "type": "string",
                "pattern": "^\\/.*",
                "description": "Route path to capture, e.g. /pricing."
              },
              "state": {
                "default": "entry",
                "description": "Label for the page state being captured.",
                "$ref": "#/$defs/__schema2"
              },
              "selection": {
                "description": "Optional label for what this evidence covers.",
                "$ref": "#/$defs/__schema3"
              }
            },
            "required": [
              "path",
              "state"
            ],
            "additionalProperties": false
          },
          "__schema2": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "__schema3": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200
          },
          "__schema4": {
            "minItems": 1,
            "maxItems": 3,
            "type": "array",
            "items": {
              "$ref": "#/$defs/__schema5"
            }
          },
          "__schema5": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "phone",
                  "tablet",
                  "desktop"
                ]
              },
              {
                "type": "object",
                "properties": {
                  "id": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 40
                  },
                  "width": {
                    "type": "integer",
                    "minimum": 240,
                    "maximum": 1920
                  },
                  "height": {
                    "type": "integer",
                    "minimum": 320,
                    "maximum": 2160
                  }
                },
                "required": [
                  "id",
                  "width",
                  "height"
                ],
                "additionalProperties": false
              }
            ]
          },
          "__schema6": {
            "type": "boolean"
          },
          "__schema7": {
            "type": "integer",
            "minimum": 10000,
            "maximum": 110000
          }
        }
      }
    },
    "forge_workspace_create": {
      "title": "Create a workspace",
      "description": "Create a disposable, isolated workspace (a container) from an authorized repository. Costly — create one only when you need to run or edit code; decide with the container-free task and read tools first. By default this call waits until the workspace is actually usable and returns state 'ready', so there is nothing to poll — the caller does not need to loop. If the wait budget runs out it returns the real state instead, and forge_workspace_get can be used to check again. Forge verifies the requested runtime before reporting success; the maintained Sandbox image defaults to Node 24.",
      "sideEffect": "workspace",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "repository": {
            "type": "object",
            "properties": {
              "provider": {
                "$ref": "#/$defs/__schema0"
              },
              "owner": {
                "$ref": "#/$defs/__schema1"
              },
              "name": {
                "$ref": "#/$defs/__schema2"
              }
            },
            "required": [
              "provider",
              "owner",
              "name"
            ],
            "additionalProperties": false,
            "description": "Authorized GitHub repository to act on."
          },
          "ref": {
            "default": "main",
            "description": "Branch or ref to check out.",
            "$ref": "#/$defs/__schema3"
          },
          "runtime": {
            "default": "node-24",
            "description": "Runtime image for the container.",
            "$ref": "#/$defs/__schema4"
          },
          "persistence": {
            "default": "ephemeral",
            "description": "Workspaces are always ephemeral.",
            "$ref": "#/$defs/__schema5"
          },
          "bootstrap": {
            "default": true,
            "description": "Install dependencies on create.",
            "$ref": "#/$defs/__schema6"
          },
          "wait_for_ready": {
            "default": true,
            "description": "Wait for the workspace to become usable before returning, so no polling loop is needed. Set false to return immediately in state 'requested'.",
            "$ref": "#/$defs/__schema7"
          },
          "wait_budget_ms": {
            "default": 60000,
            "description": "How long to wait for readiness before returning the current state anyway.",
            "$ref": "#/$defs/__schema8"
          },
          "idempotency_key": {
            "description": "Optional. Unique key that makes this mutation safe to retry; supply a stable value to make a retry idempotent, or omit it to always execute.",
            "$ref": "#/$defs/__schema9"
          }
        },
        "required": [
          "repository",
          "ref",
          "runtime",
          "persistence",
          "bootstrap",
          "wait_for_ready",
          "wait_budget_ms"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "const": "github"
          },
          "__schema1": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "__schema2": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "__schema3": {
            "type": "string"
          },
          "__schema4": {
            "type": "string",
            "enum": [
              "node-22",
              "node-24",
              "python-3.13",
              "general-purpose"
            ]
          },
          "__schema5": {
            "type": "string",
            "const": "ephemeral"
          },
          "__schema6": {
            "type": "boolean"
          },
          "__schema7": {
            "type": "boolean"
          },
          "__schema8": {
            "type": "integer",
            "minimum": 5000,
            "maximum": 110000
          },
          "__schema9": {
            "type": "string",
            "minLength": 8,
            "maxLength": 200,
            "description": "Unique key that makes this mutation safe to retry; use a fresh value per distinct call."
          }
        }
      }
    },
    "forge_workspace_get": {
      "title": "Get a workspace",
      "description": "Get a compact, reliable workspace summary: branch, head, revision, dependency state, active processes, Git integrity, and allowedNextActions. Prefer compact:true in tight ChatGPT contexts. Read-only.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "compact": {
            "description": "Return a token-efficient compact summary (recommended for ChatGPT).",
            "$ref": "#/$defs/__schema1"
          }
        },
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "boolean"
          }
        }
      }
    },
    "forge_context_get": {
      "title": "Select repository context",
      "description": "Rank the repository files most relevant to a goal, deterministically (no embeddings, no model). Returns paths with reasons, governing instructions, adjacent tests, package context and warnings — never file contents; you decide what to read. Cheap.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "goal": {
            "type": "string",
            "minLength": 1,
            "maxLength": 2000,
            "description": "What you are trying to do; drives the ranking."
          },
          "task_id": {
            "description": "Optional task to associate the lookup with.",
            "$ref": "#/$defs/__schema1"
          },
          "root": {
            "default": "/workspace/repo",
            "description": "Subtree to rank within.",
            "$ref": "#/$defs/__schema2"
          },
          "max_results": {
            "default": 12,
            "description": "Maximum number of files to return.",
            "$ref": "#/$defs/__schema3"
          },
          "categories": {
            "description": "Restrict results to these file categories.",
            "$ref": "#/$defs/__schema4"
          },
          "compact": {
            "description": "Return a token-efficient compact summary.",
            "$ref": "#/$defs/__schema6"
          }
        },
        "required": [
          "goal",
          "root",
          "max_results"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "string",
            "pattern": "^task_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target task id (task_...)."
          },
          "__schema2": {
            "type": "string",
            "pattern": "^\\/workspace.*"
          },
          "__schema3": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          },
          "__schema4": {
            "maxItems": 4,
            "type": "array",
            "items": {
              "$ref": "#/$defs/__schema5"
            }
          },
          "__schema5": {
            "type": "string",
            "enum": [
              "source",
              "tests",
              "docs",
              "config"
            ]
          },
          "__schema6": {
            "type": "boolean"
          }
        }
      }
    },
    "forge_diff_metadata": {
      "title": "Summarize the outgoing diff",
      "description": "Summarize the outgoing diff into deterministic, compact metadata: changed files, additions/deletions, changed exports, tests, config, migrations, possible secret exposure, risk areas and the files worth reading first, plus targeted verification suggestions. Syntax-only and cheap — inspect the raw diff with forge_git_outgoing_diff before any Git mutation.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "base": {
            "default": "main",
            "description": "Base branch to diff the current branch against.",
            "$ref": "#/$defs/__schema1"
          },
          "compact": {
            "description": "Return a token-efficient compact summary.",
            "$ref": "#/$defs/__schema2"
          }
        },
        "required": [
          "base"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "string",
            "minLength": 1,
            "maxLength": 255
          },
          "__schema2": {
            "type": "boolean"
          }
        }
      }
    },
    "forge_files_tree": {
      "title": "List workspace files",
      "description": "List a bounded, Git-aware file tree under /workspace. Read-only; needs a ready workspace.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "path": {
            "default": "/workspace/repo",
            "description": "Directory to list.",
            "$ref": "#/$defs/__schema1"
          },
          "depth": {
            "default": 4,
            "description": "How many levels deep to descend.",
            "$ref": "#/$defs/__schema2"
          },
          "limit": {
            "default": 1000,
            "description": "Maximum number of entries to return.",
            "$ref": "#/$defs/__schema3"
          }
        },
        "required": [
          "path",
          "depth",
          "limit"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "string",
            "pattern": "^\\/workspace.*"
          },
          "__schema2": {
            "type": "integer",
            "minimum": 1,
            "maximum": 20
          },
          "__schema3": {
            "type": "integer",
            "minimum": 1,
            "maximum": 10000
          }
        }
      }
    },
    "forge_files_read": {
      "title": "Read workspace files",
      "description": "Read one file (path) or several at once (paths) as bounded text, each with a content hash for conflict-safe edits. Reading several in one call saves round trips; start_line/end_line and max_bytes apply to each file. Needs a ready workspace.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "path": {
            "description": "Single file to read.",
            "$ref": "#/$defs/__schema1"
          },
          "paths": {
            "description": "Several files to read in one call.",
            "$ref": "#/$defs/__schema2"
          },
          "start_line": {
            "description": "First line to include (1-based).",
            "$ref": "#/$defs/__schema4"
          },
          "end_line": {
            "description": "Last line to include (1-based).",
            "$ref": "#/$defs/__schema5"
          },
          "max_bytes": {
            "default": 200000,
            "description": "Per-file byte ceiling.",
            "$ref": "#/$defs/__schema6"
          },
          "compact": {
            "description": "Omit verbose wrapper metadata for tight context windows.",
            "$ref": "#/$defs/__schema7"
          }
        },
        "required": [
          "max_bytes"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "string",
            "pattern": "^\\/workspace.*"
          },
          "__schema2": {
            "minItems": 1,
            "maxItems": 20,
            "type": "array",
            "items": {
              "$ref": "#/$defs/__schema3"
            }
          },
          "__schema3": {
            "type": "string",
            "pattern": "^\\/workspace.*"
          },
          "__schema4": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 9007199254740991
          },
          "__schema5": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 9007199254740991
          },
          "__schema6": {
            "type": "integer",
            "minimum": 1,
            "maximum": 500000
          },
          "__schema7": {
            "type": "boolean"
          }
        }
      }
    },
    "forge_files_write": {
      "title": "Write workspace file",
      "description": "Atomically create or replace one workspace file, then re-read it from the same filesystem before acknowledging success. Pass expected_sha256 for a conflict-safe overwrite. The result includes previous/resulting hashes, workspace revision and worktree hash.",
      "sideEffect": "workspace",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "path": {
            "type": "string",
            "maxLength": 1000,
            "pattern": "^\\/workspace\\/repo\\/.*",
            "description": "File to create or overwrite inside the repository."
          },
          "content": {
            "type": "string",
            "maxLength": 4000000,
            "description": "Full new file content."
          },
          "expected_sha256": {
            "description": "Hash the file must currently have, for a conflict-safe overwrite; omit to create.",
            "$ref": "#/$defs/__schema1"
          },
          "expected_revision": {
            "description": "Optimistic-concurrency guard: the revision you expect; the call fails if the workspace or task has moved on.",
            "$ref": "#/$defs/__schema2"
          },
          "idempotency_key": {
            "description": "Optional. Unique key that makes this mutation safe to retry; supply a stable value to make a retry idempotent, or omit it to always execute.",
            "$ref": "#/$defs/__schema3"
          }
        },
        "required": [
          "path",
          "content"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          },
          "__schema2": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 9007199254740991
          },
          "__schema3": {
            "type": "string",
            "minLength": 8,
            "maxLength": 200,
            "description": "Unique key that makes this mutation safe to retry; use a fresh value per distinct call."
          }
        }
      }
    },
    "forge_files_patch": {
      "title": "Apply a file patch",
      "description": "Apply a unified diff to the repository. Best for surgical multi-hunk edits; for whole-file rewrites prefer forge_files_write (agents often miscount diff context). idempotency_key must be unique per distinct patch.",
      "sideEffect": "workspace",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "patch": {
            "type": "string",
            "minLength": 1,
            "maxLength": 1000000,
            "description": "Unified diff to apply."
          },
          "idempotency_key": {
            "description": "Optional. Unique key that makes this mutation safe to retry; supply a stable value to make a retry idempotent, or omit it to always execute.",
            "$ref": "#/$defs/__schema1"
          }
        },
        "required": [
          "patch"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "string",
            "minLength": 8,
            "maxLength": 200,
            "description": "Unique key that makes this mutation safe to retry; use a fresh value per distinct call."
          }
        }
      }
    },
    "forge_shell_exec": {
      "title": "Run a command",
      "description": "Run a command in the workspace with timeout, output and network-policy bounds. Set mode:read_only for probes that must never checkpoint or take the mutation lock. Set async: true to run as a non-blocking process immediately and return proc_ id to avoid HTTP timeouts on long commands. Risky commands return a real user approval URL; idempotency_key must be unique per distinct command.",
      "sideEffect": "workspace",
      "approval": "policy",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "command": {
            "type": "string",
            "minLength": 1,
            "maxLength": 16384,
            "description": "Command to run."
          },
          "cwd": {
            "default": "/workspace/repo",
            "description": "Working directory inside the workspace; must start with /workspace.",
            "$ref": "#/$defs/__schema1"
          },
          "timeout_ms": {
            "default": 300000,
            "description": "Kill the command after this many milliseconds.",
            "$ref": "#/$defs/__schema2"
          },
          "environment": {
            "default": {},
            "description": "Extra environment variables.",
            "$ref": "#/$defs/__schema3"
          },
          "network_policy": {
            "default": "development",
            "description": "Outbound network policy for this command.",
            "$ref": "#/$defs/__schema4"
          },
          "output_limit_bytes": {
            "default": 200000,
            "description": "Truncate captured output past this many bytes.",
            "$ref": "#/$defs/__schema5"
          },
          "mode": {
            "description": "Force read-only inspection (no checkpoint, no revision bump, not blocked by active-process snapshot lock) or mutating execution. When omitted, Forge classifies the command.",
            "$ref": "#/$defs/__schema6"
          },
          "async": {
            "description": "Set true to start as a background process immediately and return proc_ id in <300ms to avoid chat transport timeout.",
            "$ref": "#/$defs/__schema7"
          },
          "compact": {
            "description": "Return compact output for tight context.",
            "$ref": "#/$defs/__schema8"
          },
          "expected_revision": {
            "description": "Optimistic-concurrency guard: the revision you expect; the call fails if the workspace or task has moved on.",
            "$ref": "#/$defs/__schema9"
          },
          "idempotency_key": {
            "description": "Optional. Unique key that makes this mutation safe to retry; supply a stable value to make a retry idempotent, or omit it to always execute.",
            "$ref": "#/$defs/__schema10"
          },
          "approval_id": {
            "description": "Approval id returned by a prior blocked call.",
            "$ref": "#/$defs/__schema11"
          }
        },
        "required": [
          "command",
          "cwd",
          "timeout_ms",
          "environment",
          "network_policy",
          "output_limit_bytes"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "string",
            "pattern": "^\\/workspace.*"
          },
          "__schema2": {
            "type": "integer",
            "minimum": 100,
            "maximum": 900000
          },
          "__schema3": {
            "type": "object",
            "propertyNames": {
              "type": "string"
            },
            "additionalProperties": {
              "type": "string"
            }
          },
          "__schema4": {
            "type": "string",
            "enum": [
              "deny_all",
              "package_install",
              "development",
              "custom_allowlist",
              "unrestricted_with_approval"
            ]
          },
          "__schema5": {
            "type": "integer",
            "minimum": 1000,
            "maximum": 1000000
          },
          "__schema6": {
            "type": "string",
            "enum": [
              "read_only",
              "mutating"
            ]
          },
          "__schema7": {
            "type": "boolean"
          },
          "__schema8": {
            "type": "boolean"
          },
          "__schema9": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 9007199254740991
          },
          "__schema10": {
            "type": "string",
            "minLength": 8,
            "maxLength": 200,
            "description": "Unique key that makes this mutation safe to retry; use a fresh value per distinct call."
          },
          "__schema11": {
            "type": "string",
            "pattern": "^apr_.*"
          }
        }
      }
    },
    "forge_process_start": {
      "title": "Start a background process",
      "description": "Start a managed background process and return its Forge process id immediately. Use for long-running work that must outlive the chat transport — dependency installs (pnpm/npm/yarn), builds, tests, and development servers. Returns mutatesFilesystem when the command writes the workspace. Prefer forge_process_wait afterwards. Dependency installs may auto-approve or return an approval URL like forge_shell_exec. idempotency_key must be unique per distinct process.",
      "sideEffect": "workspace",
      "approval": "policy",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "command": {
            "type": "string",
            "minLength": 1,
            "maxLength": 16384,
            "description": "Command to run in the background."
          },
          "cwd": {
            "default": "/workspace/repo",
            "description": "Working directory inside the workspace; must start with /workspace.",
            "$ref": "#/$defs/__schema1"
          },
          "environment": {
            "default": {},
            "description": "Extra environment variables. Forge always injects non-interactive defaults (CI, Corepack, Git).",
            "$ref": "#/$defs/__schema2"
          },
          "network_policy": {
            "default": "development",
            "description": "Outbound network policy for this process.",
            "$ref": "#/$defs/__schema3"
          },
          "expected_revision": {
            "description": "Optimistic-concurrency guard: the revision you expect; the call fails if the workspace or task has moved on.",
            "$ref": "#/$defs/__schema4"
          },
          "idempotency_key": {
            "description": "Optional. Unique key that makes this mutation safe to retry; supply a stable value to make a retry idempotent, or omit it to always execute.",
            "$ref": "#/$defs/__schema5"
          },
          "approval_id": {
            "description": "Approval id returned by a prior blocked call.",
            "$ref": "#/$defs/__schema6"
          }
        },
        "required": [
          "command",
          "cwd",
          "environment",
          "network_policy"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "string",
            "pattern": "^\\/workspace.*"
          },
          "__schema2": {
            "type": "object",
            "propertyNames": {
              "type": "string"
            },
            "additionalProperties": {
              "type": "string"
            }
          },
          "__schema3": {
            "type": "string",
            "enum": [
              "deny_all",
              "package_install",
              "development",
              "custom_allowlist"
            ]
          },
          "__schema4": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 9007199254740991
          },
          "__schema5": {
            "type": "string",
            "minLength": 8,
            "maxLength": 200,
            "description": "Unique key that makes this mutation safe to retry; use a fresh value per distinct call."
          },
          "__schema6": {
            "type": "string",
            "pattern": "^apr_.*"
          }
        }
      }
    },
    "forge_process_logs": {
      "title": "Read process logs",
      "description": "Read only the NEW log bytes after an opaque cursor, plus process status and exit code. Pass nextCursor back until hasMore is false. Read-only.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "process_id": {
            "type": "string",
            "pattern": "^proc_.*",
            "description": "Process id from forge_process_start."
          },
          "cursor": {
            "description": "Opaque cursor from a prior page's nextCursor. Omit for the first page.",
            "$ref": "#/$defs/__schema1"
          }
        },
        "required": [
          "process_id"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "string"
          }
        }
      }
    },
    "forge_process_get": {
      "title": "Get managed process status",
      "description": "Read the actual managed process status, command, working directory, mutatesFilesystem flag, and PID for one workspace-owned process.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "process_id": {
            "type": "string",
            "pattern": "^proc_.*"
          }
        },
        "required": [
          "process_id"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          }
        }
      }
    },
    "forge_process_list": {
      "title": "List managed processes",
      "description": "List all workspace-owned managed processes with status, exit codes, and whether filesystem effects were checkpointed. Read-only.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          }
        },
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          }
        }
      }
    },
    "forge_process_stop": {
      "title": "Stop managed process",
      "description": "Stop one explicit workspace-owned managed process. Forge never guesses by port or process name. No approval required — this is workspace-local recovery.",
      "sideEffect": "workspace",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "process_id": {
            "type": "string",
            "pattern": "^proc_.*"
          },
          "expected_revision": {
            "description": "Optimistic-concurrency guard: the revision you expect; the call fails if the workspace or task has moved on.",
            "$ref": "#/$defs/__schema1"
          },
          "idempotency_key": {
            "description": "Optional. Unique key that makes this mutation safe to retry; supply a stable value to make a retry idempotent, or omit it to always execute.",
            "$ref": "#/$defs/__schema2"
          }
        },
        "required": [
          "process_id"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 9007199254740991
          },
          "__schema2": {
            "type": "string",
            "minLength": 8,
            "maxLength": 200,
            "description": "Unique key that makes this mutation safe to retry; use a fresh value per distinct call."
          }
        }
      }
    },
    "forge_process_wait": {
      "title": "Wait for managed process",
      "description": "Wait server-side for a managed process to reach a terminal state (exit, failure, or cancellation), or time out. Returns the final process record, dependencyState, and whether filesystem changes were committed. Prefer this over polling forge_process_logs. Wait timeouts are observational (process is NOT killed) and return timedOut:true with suggestedTimeoutMs — retry with a larger timeout_ms (use >=600000 for large installs). Do not restart the command.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "process_id": {
            "type": "string",
            "pattern": "^proc_.*",
            "description": "Process id from forge_process_start."
          },
          "timeout_ms": {
            "default": 120000,
            "description": "Maximum time to wait for the process to finish.",
            "$ref": "#/$defs/__schema1"
          }
        },
        "required": [
          "process_id",
          "timeout_ms"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "integer",
            "minimum": 1000,
            "maximum": 600000
          }
        }
      }
    },
    "forge_process_cancel": {
      "title": "Cancel managed process",
      "description": "Cancel (SIGTERM then SIGKILL) one explicit workspace-owned managed process. The process is marked as cancelled and removed from the workspace process table. Use this instead of forge_process_stop when you want to signal that the process was intentionally aborted rather than gracefully stopped. No approval required.",
      "sideEffect": "workspace",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "process_id": {
            "type": "string",
            "pattern": "^proc_.*"
          },
          "expected_revision": {
            "description": "Optimistic-concurrency guard: the revision you expect; the call fails if the workspace or task has moved on.",
            "$ref": "#/$defs/__schema1"
          },
          "idempotency_key": {
            "description": "Optional. Unique key that makes this mutation safe to retry; supply a stable value to make a retry idempotent, or omit it to always execute.",
            "$ref": "#/$defs/__schema2"
          }
        },
        "required": [
          "process_id"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 9007199254740991
          },
          "__schema2": {
            "type": "string",
            "minLength": 8,
            "maxLength": 200,
            "description": "Unique key that makes this mutation safe to retry; use a fresh value per distinct call."
          }
        }
      }
    },
    "forge_check_start": {
      "title": "Start durable workspace check",
      "description": "Start a long-running validation command as a managed workspace check and return immediately with a process id, commit and durable status/log handle.",
      "sideEffect": "workspace",
      "approval": "policy",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "command": {
            "type": "string",
            "minLength": 1,
            "maxLength": 16384
          },
          "cwd": {
            "default": "/workspace/repo",
            "description": "Working directory inside the workspace; must start with /workspace.",
            "$ref": "#/$defs/__schema1"
          },
          "environment": {
            "default": {},
            "type": "object",
            "propertyNames": {
              "type": "string"
            },
            "additionalProperties": {
              "type": "string"
            }
          },
          "network_policy": {
            "default": "development",
            "type": "string",
            "enum": [
              "deny_all",
              "package_install",
              "development",
              "custom_allowlist"
            ]
          },
          "expected_revision": {
            "description": "Optimistic-concurrency guard: the revision you expect; the call fails if the workspace or task has moved on.",
            "$ref": "#/$defs/__schema2"
          },
          "idempotency_key": {
            "description": "Optional. Unique key that makes this mutation safe to retry; supply a stable value to make a retry idempotent, or omit it to always execute.",
            "$ref": "#/$defs/__schema3"
          }
        },
        "required": [
          "name",
          "command",
          "cwd",
          "environment",
          "network_policy"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "string",
            "pattern": "^\\/workspace.*"
          },
          "__schema2": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 9007199254740991
          },
          "__schema3": {
            "type": "string",
            "minLength": 8,
            "maxLength": 200,
            "description": "Unique key that makes this mutation safe to retry; use a fresh value per distinct call."
          }
        }
      }
    },
    "forge_check_get": {
      "title": "Get workspace check",
      "description": "Read current status, command, commit and process details for a managed validation check.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "process_id": {
            "type": "string",
            "pattern": "^proc_.*"
          }
        },
        "required": [
          "process_id"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          }
        }
      }
    },
    "forge_check_cancel": {
      "title": "Cancel workspace check",
      "description": "Cancel one managed validation check and persist that cancellation.",
      "sideEffect": "workspace",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "process_id": {
            "type": "string",
            "pattern": "^proc_.*"
          },
          "expected_revision": {
            "description": "Optimistic-concurrency guard: the revision you expect; the call fails if the workspace or task has moved on.",
            "$ref": "#/$defs/__schema1"
          },
          "idempotency_key": {
            "description": "Optional. Unique key that makes this mutation safe to retry; supply a stable value to make a retry idempotent, or omit it to always execute.",
            "$ref": "#/$defs/__schema2"
          }
        },
        "required": [
          "process_id"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 9007199254740991
          },
          "__schema2": {
            "type": "string",
            "minLength": 8,
            "maxLength": 200,
            "description": "Unique key that makes this mutation safe to retry; use a fresh value per distinct call."
          }
        }
      }
    },
    "forge_dependencies_install": {
      "title": "Install workspace dependencies",
      "description": "Start dependency install as a managed background process and return quickly with processId when the install outlives the chat transport. If still running, call forge_process_wait with timeout_ms >= 600000 — do not start a second install. Fast installs may complete inside this call.",
      "sideEffect": "workspace",
      "approval": "policy",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "frozen_lockfile": {
            "default": true,
            "description": "Use --frozen-lockfile / --immutable when true (fails if the lockfile is out of sync). Set false to allow lockfile updates.",
            "$ref": "#/$defs/__schema1"
          },
          "allow_lockfile_update": {
            "default": false,
            "description": "Allow the install to update the lockfile when it is out of sync with the manifest.",
            "$ref": "#/$defs/__schema2"
          },
          "network_policy": {
            "default": "package_install",
            "description": "Outbound network policy for this install.",
            "$ref": "#/$defs/__schema3"
          },
          "timeout_ms": {
            "default": 600000,
            "description": "Suggested overall wait budget for a follow-up forge_process_wait.",
            "$ref": "#/$defs/__schema4"
          },
          "expected_revision": {
            "description": "Optimistic-concurrency guard: the revision you expect; the call fails if the workspace or task has moved on.",
            "$ref": "#/$defs/__schema5"
          },
          "idempotency_key": {
            "description": "Optional. Unique key that makes this mutation safe to retry; supply a stable value to make a retry idempotent, or omit it to always execute.",
            "$ref": "#/$defs/__schema6"
          }
        },
        "required": [
          "frozen_lockfile",
          "allow_lockfile_update",
          "network_policy",
          "timeout_ms"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "boolean"
          },
          "__schema2": {
            "type": "boolean"
          },
          "__schema3": {
            "type": "string",
            "enum": [
              "deny_all",
              "package_install",
              "development",
              "custom_allowlist",
              "unrestricted_with_approval"
            ]
          },
          "__schema4": {
            "type": "integer",
            "minimum": 10000,
            "maximum": 900000
          },
          "__schema5": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 9007199254740991
          },
          "__schema6": {
            "type": "string",
            "minLength": 8,
            "maxLength": 200,
            "description": "Unique key that makes this mutation safe to retry; use a fresh value per distinct call."
          }
        }
      }
    },
    "forge_git_status": {
      "title": "Read Git status",
      "description": "Get the workspace repository's working-tree and branch status. Read-only.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          }
        },
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          }
        }
      }
    },
    "forge_git_diff": {
      "title": "Read Git diff",
      "description": "Get the working-tree (or staged) diff one page of files at a time. Every call returns the COMPLETE list of changed files with line counts; `diff` carries the hunks for that page only. Follow `nextCursor` for the rest, or pass `paths` to jump straight to specific files. Read-only.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "staged": {
            "default": false,
            "description": "Diff staged changes instead of the working tree.",
            "$ref": "#/$defs/__schema1"
          },
          "cursor": {
            "description": "Opaque cursor from a prior page's nextCursor. Omit for the first page.",
            "$ref": "#/$defs/__schema2"
          },
          "max_bytes": {
            "default": 64000,
            "description": "Approximate byte budget for this page of hunks. Lower it if results are still too large to read comfortably.",
            "$ref": "#/$defs/__schema3"
          },
          "paths": {
            "description": "Restrict the diff to these exact paths (from `files`). Use this to read one large file's change directly instead of paging to it.",
            "$ref": "#/$defs/__schema4"
          },
          "compact": {
            "description": "Return compact hunk representation.",
            "$ref": "#/$defs/__schema6"
          }
        },
        "required": [
          "staged",
          "max_bytes"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "boolean"
          },
          "__schema2": {
            "type": "string",
            "maxLength": 64
          },
          "__schema3": {
            "type": "integer",
            "minimum": 2000,
            "maximum": 400000
          },
          "__schema4": {
            "maxItems": 200,
            "type": "array",
            "items": {
              "$ref": "#/$defs/__schema5"
            }
          },
          "__schema5": {
            "type": "string",
            "minLength": 1,
            "maxLength": 400
          },
          "__schema6": {
            "type": "boolean"
          }
        }
      }
    },
    "forge_git_branch_create": {
      "title": "Create a branch",
      "description": "Create and check out a local branch under the required forge/ namespace.",
      "sideEffect": "workspace",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "branch": {
            "type": "string",
            "maxLength": 107,
            "pattern": "^forge\\/.*",
            "description": "Branch name; must start with forge/."
          },
          "expected_revision": {
            "description": "Optimistic-concurrency guard: the revision you expect; the call fails if the workspace or task has moved on.",
            "$ref": "#/$defs/__schema1"
          },
          "idempotency_key": {
            "description": "Optional. Unique key that makes this mutation safe to retry; supply a stable value to make a retry idempotent, or omit it to always execute.",
            "$ref": "#/$defs/__schema2"
          }
        },
        "required": [
          "branch"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 9007199254740991
          },
          "__schema2": {
            "type": "string",
            "minLength": 8,
            "maxLength": 200,
            "description": "Unique key that makes this mutation safe to retry; use a fresh value per distinct call."
          }
        }
      }
    },
    "forge_git_commit": {
      "title": "Commit changes",
      "description": "Stage the given repository paths and commit them, attributed to forge-mcp[bot]. Omit message to auto-generate a conventional-commit message from the diff.",
      "sideEffect": "workspace",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "message": {
            "description": "Commit message; omit to auto-generate.",
            "$ref": "#/$defs/__schema1"
          },
          "paths": {
            "default": [],
            "description": "Paths to stage; empty stages all changes.",
            "$ref": "#/$defs/__schema2"
          },
          "expected_revision": {
            "description": "Optimistic-concurrency guard: the revision you expect; the call fails if the workspace or task has moved on.",
            "$ref": "#/$defs/__schema4"
          },
          "idempotency_key": {
            "description": "Optional. Unique key that makes this mutation safe to retry; supply a stable value to make a retry idempotent, or omit it to always execute.",
            "$ref": "#/$defs/__schema5"
          }
        },
        "required": [
          "paths"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "string",
            "minLength": 1,
            "maxLength": 500
          },
          "__schema2": {
            "maxItems": 100,
            "type": "array",
            "items": {
              "$ref": "#/$defs/__schema3"
            }
          },
          "__schema3": {
            "type": "string",
            "minLength": 1,
            "maxLength": 500
          },
          "__schema4": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 9007199254740991
          },
          "__schema5": {
            "type": "string",
            "minLength": 8,
            "maxLength": 200,
            "description": "Unique key that makes this mutation safe to retry; use a fresh value per distinct call."
          }
        }
      }
    },
    "forge_git_outgoing_diff": {
      "title": "Inspect the outgoing change",
      "description": "Get the diff between the base branch and the current forge/ branch, one page of files at a time. Every call returns the COMPLETE list of changed files plus `diffHash` over the WHOLE change (safe to pass to push from any page); `diff` carries the hunks for that page only. Follow `nextCursor` for the rest, or pass `paths` for specific files. Read-only — inspect this before requesting a push.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "base": {
            "default": "main",
            "description": "Base branch to compare against.",
            "$ref": "#/$defs/__schema1"
          },
          "cursor": {
            "description": "Opaque cursor from a prior page's nextCursor. Omit for the first page.",
            "$ref": "#/$defs/__schema2"
          },
          "max_bytes": {
            "default": 64000,
            "description": "Approximate byte budget for this page of hunks. Lower it if results are still too large to read comfortably.",
            "$ref": "#/$defs/__schema3"
          },
          "paths": {
            "description": "Restrict the diff to these exact paths (from `files`). Use this to read one large file's change directly instead of paging to it.",
            "$ref": "#/$defs/__schema4"
          },
          "compact": {
            "description": "Return compact hunk representation.",
            "$ref": "#/$defs/__schema6"
          }
        },
        "required": [
          "base",
          "max_bytes"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "string",
            "minLength": 1,
            "maxLength": 255
          },
          "__schema2": {
            "type": "string",
            "maxLength": 64
          },
          "__schema3": {
            "type": "integer",
            "minimum": 2000,
            "maximum": 400000
          },
          "__schema4": {
            "maxItems": 200,
            "type": "array",
            "items": {
              "$ref": "#/$defs/__schema5"
            }
          },
          "__schema5": {
            "type": "string",
            "minLength": 1,
            "maxLength": 400
          },
          "__schema6": {
            "type": "boolean"
          }
        }
      }
    },
    "forge_submit_for_review": {
      "title": "Submit finished work for review",
      "description": "Finish a piece of work in one call, without waiting for anyone. Stages the current branch on a Forge-owned ref, queues a pull request for it, and returns immediately — the human approves whenever they like, from the Forge portal or the returned URL, and Forge performs the push and opens the draft PR itself. Prefer this over forge_git_push + forge_pull_request_create, which both block until a human clicks. Any uncommitted work is committed automatically, so this is genuinely one call. The workspace can be destroyed straight after this returns; the staged commits outlive it.",
      "sideEffect": "external",
      "approval": "deferred",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "branch": {
            "type": "string",
            "maxLength": 107,
            "pattern": "^forge\\/.*",
            "description": "forge/ branch the work should land on once approved."
          },
          "base": {
            "default": "main",
            "description": "Branch the pull request will target.",
            "$ref": "#/$defs/__schema1"
          },
          "title": {
            "description": "PR title; omit to auto-generate from the diff.",
            "$ref": "#/$defs/__schema2"
          },
          "body": {
            "default": "",
            "description": "PR body.",
            "$ref": "#/$defs/__schema3"
          },
          "task_id": {
            "description": "Task this submission completes, for traceability.",
            "$ref": "#/$defs/__schema4"
          },
          "idempotency_key": {
            "description": "Optional. Unique key that makes this mutation safe to retry; supply a stable value to make a retry idempotent, or omit it to always execute.",
            "$ref": "#/$defs/__schema5"
          }
        },
        "required": [
          "branch",
          "base",
          "body"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "string",
            "minLength": 1,
            "maxLength": 255
          },
          "__schema2": {
            "type": "string",
            "minLength": 1,
            "maxLength": 256
          },
          "__schema3": {
            "type": "string",
            "maxLength": 60000
          },
          "__schema4": {
            "type": "string",
            "pattern": "^task_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target task id (task_...)."
          },
          "__schema5": {
            "type": "string",
            "minLength": 8,
            "maxLength": 200,
            "description": "Unique key that makes this mutation safe to retry; use a fresh value per distinct call."
          }
        }
      }
    },
    "forge_git_push": {
      "title": "Push a branch",
      "description": "Push a non-default forge/ branch through the GitHub App credential proxy. Requires a real user approval page and the expected diff hash.",
      "sideEffect": "external",
      "approval": "required",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "branch": {
            "type": "string",
            "maxLength": 107,
            "pattern": "^forge\\/.*",
            "description": "forge/ branch to push."
          },
          "base": {
            "default": "main",
            "description": "Base branch the push targets.",
            "$ref": "#/$defs/__schema1"
          },
          "expected_diff_hash": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$",
            "description": "diffHash from forge_git_outgoing_diff; the push fails if the diff changed."
          },
          "approval_id": {
            "description": "Approval id from the approval page.",
            "$ref": "#/$defs/__schema2"
          },
          "expected_revision": {
            "description": "Optimistic-concurrency guard: the revision you expect; the call fails if the workspace or task has moved on.",
            "$ref": "#/$defs/__schema3"
          },
          "idempotency_key": {
            "description": "Optional. Unique key that makes this mutation safe to retry; supply a stable value to make a retry idempotent, or omit it to always execute.",
            "$ref": "#/$defs/__schema4"
          }
        },
        "required": [
          "branch",
          "base",
          "expected_diff_hash"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "string",
            "minLength": 1,
            "maxLength": 255
          },
          "__schema2": {
            "type": "string",
            "pattern": "^apr_.*"
          },
          "__schema3": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 9007199254740991
          },
          "__schema4": {
            "type": "string",
            "minLength": 8,
            "maxLength": 200,
            "description": "Unique key that makes this mutation safe to retry; use a fresh value per distinct call."
          }
        }
      }
    },
    "forge_pull_request_create": {
      "title": "Open a draft pull request",
      "description": "Open a draft GitHub pull request for an already-pushed forge/ branch. Requires a real user approval page. Omit title to auto-generate the title and body from the branch diff.",
      "sideEffect": "external",
      "approval": "required",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "head": {
            "type": "string",
            "maxLength": 107,
            "pattern": "^forge\\/.*",
            "description": "forge/ branch to open the PR from."
          },
          "base": {
            "default": "main",
            "description": "Branch to merge into.",
            "$ref": "#/$defs/__schema1"
          },
          "title": {
            "description": "PR title; omit to auto-generate.",
            "$ref": "#/$defs/__schema2"
          },
          "body": {
            "default": "",
            "description": "PR body.",
            "$ref": "#/$defs/__schema3"
          },
          "approval_id": {
            "description": "Approval id from the approval page.",
            "$ref": "#/$defs/__schema4"
          }
        },
        "required": [
          "head",
          "base",
          "body"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "string",
            "minLength": 1,
            "maxLength": 255
          },
          "__schema2": {
            "type": "string",
            "minLength": 1,
            "maxLength": 256
          },
          "__schema3": {
            "type": "string",
            "maxLength": 60000
          },
          "__schema4": {
            "type": "string",
            "pattern": "^apr_.*"
          }
        }
      }
    },
    "forge_task_authorize_push_envelope": {
      "title": "Pre-authorize a run of pushes",
      "description": "Ask a human to authorize, ONCE, a bounded envelope covering the next several forge_git_push calls to one branch — for tasks that iterate in many small commit-then-push cycles, so the human is not asked to click through every single push. A push only auto-satisfies inside the envelope while it (a) fast-forwards the same branch/base with no rewritten history, and (b) touches only paths under allowed_paths; anything else falls back to a normal, individually-approved push. Never covers forge_pull_request_create, which always needs its own approval. Requires a real user approval page, same as a push.",
      "sideEffect": "none",
      "approval": "required",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "task_id": {
            "description": "Task this envelope belongs to, for traceability.",
            "$ref": "#/$defs/__schema1"
          },
          "branch": {
            "type": "string",
            "maxLength": 107,
            "pattern": "^forge\\/.*",
            "description": "forge/ branch the envelope covers."
          },
          "base": {
            "default": "main",
            "description": "Base branch, fixed for the life of the envelope.",
            "$ref": "#/$defs/__schema2"
          },
          "allowed_paths": {
            "description": "Path prefixes the envelope covers. Omit to default to the paths in the current outgoing diff.",
            "$ref": "#/$defs/__schema3"
          },
          "ttl_minutes": {
            "default": 120,
            "description": "How long the envelope stays valid.",
            "$ref": "#/$defs/__schema5"
          },
          "approval_id": {
            "description": "Approval id from the approval page.",
            "$ref": "#/$defs/__schema6"
          }
        },
        "required": [
          "branch",
          "base",
          "ttl_minutes"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "string",
            "pattern": "^task_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target task id (task_...)."
          },
          "__schema2": {
            "type": "string",
            "minLength": 1,
            "maxLength": 255
          },
          "__schema3": {
            "minItems": 1,
            "maxItems": 50,
            "type": "array",
            "items": {
              "$ref": "#/$defs/__schema4"
            }
          },
          "__schema4": {
            "type": "string",
            "minLength": 1,
            "maxLength": 500
          },
          "__schema5": {
            "type": "integer",
            "minimum": 5,
            "maximum": 480
          },
          "__schema6": {
            "type": "string",
            "pattern": "^apr_.*"
          }
        }
      }
    },
    "forge_preview_expose": {
      "title": "Expose a preview",
      "description": "Expose a running process through a short-lived Forge preview capability. Private by default; public access requires approval.",
      "sideEffect": "workspace",
      "approval": "policy",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "process_id": {
            "type": "string",
            "pattern": "^proc_.*",
            "description": "Process to expose."
          },
          "port": {
            "type": "integer",
            "minimum": 1024,
            "maximum": 65535,
            "description": "Port the process listens on."
          },
          "access": {
            "default": "private",
            "description": "Who can reach the preview.",
            "$ref": "#/$defs/__schema1"
          },
          "ttl_seconds": {
            "default": 3600,
            "description": "How long the preview stays open.",
            "$ref": "#/$defs/__schema2"
          },
          "expected_revision": {
            "description": "Optimistic-concurrency guard: the revision you expect; the call fails if the workspace or task has moved on.",
            "$ref": "#/$defs/__schema3"
          },
          "idempotency_key": {
            "description": "Optional. Unique key that makes this mutation safe to retry; supply a stable value to make a retry idempotent, or omit it to always execute.",
            "$ref": "#/$defs/__schema4"
          }
        },
        "required": [
          "process_id",
          "port",
          "access",
          "ttl_seconds"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "string",
            "enum": [
              "private",
              "tenant",
              "share-link",
              "public"
            ]
          },
          "__schema2": {
            "type": "integer",
            "minimum": 60,
            "maximum": 86400
          },
          "__schema3": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 9007199254740991
          },
          "__schema4": {
            "type": "string",
            "minLength": 8,
            "maxLength": 200,
            "description": "Unique key that makes this mutation safe to retry; use a fresh value per distinct call."
          }
        }
      }
    },
    "forge_review_capture": {
      "title": "Capture preview review evidence",
      "description": "Screenshot your own running app in the workspace and get the images back in this call. Omit preview_id and Forge starts the dev server and exposes it for you, so this is one call rather than a start-poll-expose-capture sequence. Give a capture `steps` to drive a real interaction (click/fill/press/wait) before the shot, proving multi-step flows a static screenshot cannot. Screenshots come back attached to the result; this is the only preview-evidence tool.",
      "sideEffect": "workspace",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "preview_id": {
            "description": "Preview id from forge_preview_expose. Optional — omit it and Forge starts the project dev server and exposes it for you, which is usually what you want.",
            "$ref": "#/$defs/__schema1"
          },
          "preview_wait_ms": {
            "default": 60000,
            "description": "How long to wait for the dev server to come up when Forge is starting it.",
            "$ref": "#/$defs/__schema2"
          },
          "captures": {
            "minItems": 1,
            "maxItems": 20,
            "type": "array",
            "items": {
              "$ref": "#/$defs/__schema3"
            },
            "description": "Routes to capture."
          },
          "viewports": {
            "default": [
              "phone",
              "desktop"
            ],
            "description": "Viewports to capture each route at. Use the shorthand names 'phone', 'tablet' or 'desktop', or give explicit sizes.",
            "$ref": "#/$defs/__schema8"
          }
        },
        "required": [
          "preview_wait_ms",
          "captures",
          "viewports"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "string",
            "pattern": "^prv_.*"
          },
          "__schema2": {
            "type": "integer",
            "minimum": 5000,
            "maximum": 110000
          },
          "__schema3": {
            "type": "object",
            "properties": {
              "route": {
                "type": "string",
                "pattern": "^\\/.*",
                "description": "Route path to capture, e.g. /cart."
              },
              "state": {
                "default": "entry",
                "description": "Label for the page state being captured.",
                "$ref": "#/$defs/__schema4"
              },
              "selection": {
                "description": "Optional label for what this evidence covers.",
                "$ref": "#/$defs/__schema5"
              },
              "steps": {
                "description": "Interaction to drive before the shot.",
                "$ref": "#/$defs/__schema6"
              }
            },
            "required": [
              "route",
              "state"
            ],
            "additionalProperties": false
          },
          "__schema4": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "__schema5": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200
          },
          "__schema6": {
            "minItems": 1,
            "maxItems": 20,
            "type": "array",
            "items": {
              "$ref": "#/$defs/__schema7"
            }
          },
          "__schema7": {
            "type": "object",
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "navigate",
                  "click",
                  "fill",
                  "press",
                  "wait_for_selector",
                  "wait_for_text",
                  "wait",
                  "reload"
                ]
              },
              "selector": {
                "type": "string",
                "minLength": 1,
                "maxLength": 1000
              },
              "value": {
                "type": "string",
                "maxLength": 10000
              },
              "key": {
                "type": "string",
                "minLength": 1,
                "maxLength": 40
              },
              "text": {
                "type": "string",
                "minLength": 1,
                "maxLength": 1000
              },
              "path": {
                "type": "string",
                "pattern": "^\\/.*"
              },
              "timeout_ms": {
                "type": "integer",
                "minimum": 100,
                "maximum": 30000
              }
            },
            "required": [
              "kind"
            ],
            "additionalProperties": false
          },
          "__schema8": {
            "minItems": 1,
            "maxItems": 4,
            "type": "array",
            "items": {
              "$ref": "#/$defs/__schema9"
            }
          },
          "__schema9": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "phone",
                  "tablet",
                  "desktop"
                ]
              },
              {
                "type": "object",
                "properties": {
                  "id": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 40
                  },
                  "width": {
                    "type": "integer",
                    "minimum": 240,
                    "maximum": 3840
                  },
                  "height": {
                    "type": "integer",
                    "minimum": 240,
                    "maximum": 2160
                  }
                },
                "required": [
                  "id",
                  "width",
                  "height"
                ],
                "additionalProperties": false
              }
            ]
          }
        }
      }
    },
    "forge_artifact_get": {
      "title": "Get an artifact",
      "description": "Fetch a stored Forge artifact. Image artifacts are returned as MCP image content for direct model inspection. Read-only.",
      "sideEffect": "none",
      "approval": "none",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "artifact_id": {
            "type": "string",
            "pattern": "^art_.*",
            "description": "Artifact id, e.g. from evidence[].screenshot.artifactId."
          },
          "max_bytes": {
            "default": 2500000,
            "description": "Maximum bytes to return.",
            "$ref": "#/$defs/__schema1"
          }
        },
        "required": [
          "artifact_id",
          "max_bytes"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "integer",
            "minimum": 1,
            "maximum": 4000000
          }
        }
      }
    },
    "forge_workspace_destroy": {
      "title": "Destroy a workspace",
      "description": "Destroy a workspace: revoke previews and capabilities, stop processes and tear down the container. Do this once the task or review is complete. Refused when the repository has committed changes on a forge/ branch that were never pushed — pass force to destroy anyway and accept the loss.",
      "sideEffect": "destructive",
      "approval": "policy",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "workspace_id": {
            "description": "Target workspace id (ws_...). Optional — omit it and Forge uses your open workspace, which is what you want whenever only one is running.",
            "$ref": "#/$defs/__schema0"
          },
          "preserve_artifacts": {
            "default": true,
            "description": "Keep captured artifacts after teardown.",
            "$ref": "#/$defs/__schema1"
          },
          "force": {
            "default": false,
            "description": "Destroy even if the workspace has unpushed committed work.",
            "$ref": "#/$defs/__schema2"
          },
          "expected_revision": {
            "description": "Optimistic-concurrency guard: the revision you expect; the call fails if the workspace or task has moved on.",
            "$ref": "#/$defs/__schema3"
          },
          "idempotency_key": {
            "description": "Optional. Unique key that makes this mutation safe to retry; supply a stable value to make a retry idempotent, or omit it to always execute.",
            "$ref": "#/$defs/__schema4"
          }
        },
        "required": [
          "preserve_artifacts",
          "force"
        ],
        "additionalProperties": false,
        "$defs": {
          "__schema0": {
            "type": "string",
            "pattern": "^ws_[0-9a-hjkmnp-tv-z]{20,32}$",
            "description": "Target workspace id (ws_...)."
          },
          "__schema1": {
            "type": "boolean"
          },
          "__schema2": {
            "type": "boolean"
          },
          "__schema3": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 9007199254740991
          },
          "__schema4": {
            "type": "string",
            "minLength": 8,
            "maxLength": 200,
            "description": "Unique key that makes this mutation safe to retry; use a fresh value per distinct call."
          }
        }
      }
    }
  }
}
