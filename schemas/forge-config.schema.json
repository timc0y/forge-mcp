{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/timc0y/forge-mcp/main/schemas/forge-config.schema.json",
  "title": "Forge repository configuration",
  "description": "Optional repository-owned launch settings for Forge branch previews.",
  "type": "object",
  "properties": {
    "preview": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "cwd": {
          "type": "string",
          "minLength": 1,
          "maxLength": 500,
          "description": "Repository-relative working directory for the preview server."
        },
        "command": {
          "type": "string",
          "minLength": 1,
          "maxLength": 2000,
          "description": "Exact command Forge starts for a branch preview."
        },
        "port": {
          "type": "integer",
          "minimum": 1024,
          "maximum": 65535,
          "description": "Port exposed from the ephemeral preview executor."
        }
      }
    }
  },
  "additionalProperties": true
}
