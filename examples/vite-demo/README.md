# Vite acceptance fixture

The deployed acceptance test should clone a pinned public Vite fixture, start it on `0.0.0.0:5173`, verify hot-reload WebSockets through the Forge preview gateway and capture browser evidence.
