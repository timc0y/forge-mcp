# Parallax native client

Parallax is Forge's first-party consumer. The native service-binding path will call the same application services as MCP and will receive workspace-scoped capabilities rather than Sandbox or GitHub credentials. See `docs/architecture/parallax.md`.
