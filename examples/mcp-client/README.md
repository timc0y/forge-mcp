# MCP client example

Connect any Streamable HTTP MCP client to `/mcp`, complete OAuth discovery from `/.well-known/oauth-protected-resource`, then call `forge_workspace_create`. The generated schemas in `schemas/forge-tools.schema.json` are the canonical machine-readable tool contract.
