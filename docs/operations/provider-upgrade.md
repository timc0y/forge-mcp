# Provider upgrade procedure

Pin a candidate version on a branch. Review upstream API/security changes. Run unit, adapter contract, malicious-fixture and deployed development tests. Create/restore compatibility snapshots. Compare preview, process, command and file semantics. Record changes in the adoption register. Roll back by package/image pin if any contract differs.
