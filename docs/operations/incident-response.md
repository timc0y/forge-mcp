# Incident response

Contain: disable affected tenant/project, revoke previews/capabilities, stop sandboxes and pause external side effects. Preserve immutable audit/log artifacts with restricted access. Determine blast radius using tenant/workspace/repository scopes. Rotate credentials, remove malicious snapshots, fix policy, replay contract tests and document recovery before re-enabling.
