# MCP client compatibility

Primary contract: MCP Streamable HTTP with OAuth discovery. Phase 1 tests tool discovery and calls with MCP Inspector plus ChatGPT, Claude and one independent client before release.

MCP Apps are optional. Clients without UI support receive the same structured results and artifact references. A local stdio proxy is a transport shim only.
