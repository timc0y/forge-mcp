# MCP client compatibility

Primary contract: MCP Streamable HTTP with OAuth discovery. Automated cloud acceptance verifies discovery, PKCE, tool calls, image content and teardown. Public release also requires hands-on compatibility checks with ChatGPT, Claude and an independent MCP inspector.

MCP Apps are optional. Clients without UI support receive the same structured results and artifact references. A local stdio proxy is a transport shim only.
